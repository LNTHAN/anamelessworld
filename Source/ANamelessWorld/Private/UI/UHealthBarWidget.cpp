#include "UI/UHealthBarWidget.h"

// No logic here yet — the class is a data holder (OwnerCharacter) plus whatever
// the Blueprint child (WBP_HealthBar) wires up. A .cpp is still required so the
// class links.